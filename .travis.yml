language: node_js
node_js:
  - '8'
# branches:
#   only:
#     - master
install:
  - npm install --global now
script:
  - now rm -t $NOW_TOKEN $NAME -y
  - now deploy --docker -t $NOW_TOKEN -n $NAME -p  -e MONGO_URL=$MONGO_URL
  - now alias -t $NOW_TOKEN $NAME.now.sh